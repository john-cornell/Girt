# Project Rule: Application Versioning

Always bump and update the application version (`AppVersion` / `<Version>` in `Girt.csproj`) on EVERY change or user request so the user can immediately verify in the title bar that they have the latest build running.
